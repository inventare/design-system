import { Canvas, Meta, Story, Source } from '@storybook/blocks';
import * as ModalStories from './Modal.stories';

<Meta of={ModalStories} />

# Modal

The Modal component is created with `HTML`, `SCSS` and `JavaScript` for the open and close behaviour. A fully functional example can be show, when your click on the open button bellow:

<Story of={ModalStories.Default} />
